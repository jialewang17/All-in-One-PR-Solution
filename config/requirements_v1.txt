# 公关传播RAG系统 v1.1 依赖包列表

# 核心框架 / LLM
openai>=1.0.0
langchain>=0.1.0           # 如无需 LangChain，可注释掉
langchain-openai>=0.0.5
langchain-community>=0.0.10
tiktoken>=0.5.0

# Neo4j
neo4j>=5.14.0
# py2neo>=2021.2.3         # 如无需要，可保持注释

# 配置 / 环境
python-dotenv>=1.0.0
pyyaml>=6.0.0

# 文档处理
PyPDF2>=3.0.0
openpyxl>=3.1.0
pandas>=2.0.0
python-docx>=0.8.11
python-pptx>=0.6.21
beautifulsoup4>=4.12.0
lxml>=4.9.0

# 文本处理
nltk>=3.8.0
spacy>=3.6.0
jieba>=0.42.1

# 数据处理
numpy>=1.24.0
scikit-learn>=1.3.0

# 网络请求
requests>=2.31.0
urllib3>=2.0.0

# 日志和调试
loguru>=0.7.0

# 测试框架
pytest>=7.4.0
pytest-cov>=4.1.0

# 开发工具（可选，建议放入 requirements-dev.txt）
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# 可选依赖 (用于高级功能)
# redis>=4.6.0  # 缓存支持
# celery>=5.3.0  # 异步任务

# Web API 服务（一体化助理工具）
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# 文件监控和任务调度
watchdog>=3.0.0
apscheduler>=3.10.0

# Markdown frontmatter 支持
python-frontmatter>=1.0.0

# 异步 HTTP 客户端
aiohttp>=3.9.0

# 性能优化工具
tqdm>=4.66.0          # 进度条显示
tenacity>=8.2.0       # 重试机制
